spring.application.name=StaySearchBackend

spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update



spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

server.address=0.0.0.0

cloudinary.cloud-name=${cloudinary.cloud-name}
cloudinary.api-key=${cloudinary.api-key}
cloudinary.api-secret=${cloudinary.api-secret}

spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000

jwt.secret=${jwt.secret}
# JWT expiration time (1 hour in milliseconds)
jwt.expiration=3600000

#  Spring Mail Configuration
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=contact@valliento.tech
#spring.mail.password=xcciscrozqxgjgqo
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true


spring.jackson.time-zone=Asia/Kolkata

#SendgridConfiguration
sendgrid.api.key=${SENDGRID_API_KEY}
sendgrid.from.email=contact@valliento.tech


#Payment Gateway
stripe.secret.key=${STRIPE_SECRET_KEY}

#stripe.secret.key=##############

cashfree.client.id=${CASHFREE_CLIENT_ID}
cashfree.client.secret=${CASHFREE_CLIENT_SECRET}

#cashfree.client.id=################
#cashfree.client.secret=###############

# RazorPay
RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
RAZORPAY_WEBHOOK_SECRET=${RAZORPAY_WEBHOOK_SECRET}

# RazorPay
#RAZORPAY_KEY_ID=rzp_test_R7a4xMSxsnrixu
#RAZORPAY_KEY_SECRET=y8n9hxhBKHneR4TOVpF58Z5Q
#RAZORPAY_WEBHOOK_SECRET=Valliento.tech@1234

# Ensures the cookie is only sent over HTTPS - always add hash# on next line when on testing env
server.servlet.session.cookie.secure=true

# Prevents JavaScript access to the cookie (Fixes CWE-1004)
server.servlet.session.cookie.http-only=true

# Prevents CSRF by controlling cross-site requests (Fixes CWE-1275)
server.servlet.session.cookie.same-site=lax